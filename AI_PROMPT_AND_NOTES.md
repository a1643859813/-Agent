# AI 使用与问题解决记录

## 使用原则

本项目使用 AI 作为结对编程和审查辅助：用于拆解需求、讨论边界条件、生成测试思路和审阅说明文档。Agent Runtime 的架构取舍、代码整合、实际运行、测试结果核对及最终提交内容均由本人确认。

## 典型 Prompt 与使用方式

### 1. 拆解题目与确定最小边界

> 请将“从零实现最小可用 Agent”的需求拆为可独立验证的模块。约束：不能使用 Agent 框架，必须由本地 Runtime 实现工具循环、session 隔离、context 管理、异常处理和 trace。

采用结果：把实现拆分为 `runtime`、`llm`、`tools`、`store` 和 `tasks` 五层；模型只负责决策，工具执行和状态转换均放在本地代码中。

### 2. 审视 context 与 memory

> 一个用户有多个聊天窗口，并且会持续追问。请比较短期会话上下文、会话摘要和长期偏好的存储边界，并给出低复杂度、可测试的召回方案。

采用结果：短期消息和 todo 按 `(user_id, session_id)` 隔离；长期记忆按 `user_id` 存储，仅在用户明确说“记住”时写入。每轮请求以当前问题检索 Top-K 记忆，并按“系统提示 → 记忆 → 摘要 → 最近消息”放入 context。

### 3. 工具与异常测试

> 请为一个支持 function calling 的最小 Agent 列出高价值的离线测试场景，覆盖工具参数、会话隔离、模型输出解析、轮次上限和异常恢复。

采用结果：加入计算器 AST 白名单、未知工具/缺参处理、native/text 工具调用解析、session busy 队列、摘要降级、跨会话记忆与异步任务隔离等测试。离线测试不调用付费 API。

### 4. 真实 API 回归

> 真实 API 测试如何既验证模型能够主动选工具，又避免日常测试消耗额度？

采用结果：真实 DeepSeek 测试单独放在 `test_deepseek_regression.py`，只有设置 `RUN_DEEPSEEK_REGRESSION=1` 时执行。验证直接回答、`calculator` 选择和 `todo` 选择；2026-07-26 已通过 `deepseek-v4-flash` 实测。

## 遇到的问题与处理

| 问题 | 原因判断 | 处理方式 |
| --- | --- | --- |
| 真实 API 测试返回 400 | `deepseek-chat` 已不被当前接口接受 | 将示例和本地配置改为 `deepseek-v4-flash`，再完成真实回归验证 |
| 受限环境出现 socket 权限错误 | 本地受限环境禁止出网 | 在允许网络的环境重跑真实 API 测试；离线测试保持不依赖网络 |
| 长对话会挤占 context | 原始历史无限增长 | 保留最近消息，超出阈值后压缩早期历史为结构化摘要；失败时使用确定性文本摘要兜底 |
| 同一 session 同时到达消息 | 并发修改历史可能造成顺序错乱 | 为 session 加 `idle/busy` 状态；busy 时写入 FIFO 队列，当前任务结束后顺序 drain |

## 验证记录

- 离线测试：103 条通过；真实 API 测试默认跳过。
- 真实 DeepSeek Flash API 回归：3 条通过（直接回答、calculator、todo）。
- 测试命令与运行方式详见 [README.md](README.md)。
